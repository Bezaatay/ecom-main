import { StyleSheet } from "react-native";

export const buttonStyles = StyleSheet.create({
    container: {
        borderRadius: 50,
        alignItems: 'center',
        justifyContent: 'center',
        margin: 8
    },
    text: {
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 18
    },
});
